export const CONTRACT_ADDRESSES = {
  ticketFactory: process.env.NEXT_PUBLIC_TICKET_FACTORY as `0x${string}`,
  rewardsPool: process.env.NEXT_PUBLIC_REWARDS_POOL as `0x${string}`,
  zkVerifier: process.env.NEXT_PUBLIC_ZK_VERIFIER as `0x${string}`,
};
